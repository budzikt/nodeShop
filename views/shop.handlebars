<script type="text/javascript">
   // $(document).on("click", ".btm-modal_toggle", function (e) {
    //var dbId = this.attributes['dbid'].value
    // $('#modalTextId').after(dbId);
//  });
$(document).on("click", "div .panel-body", function (e) {
    if($(e.target).hasClass("comment-button")){
        var dbId = e.target.attributes['dbid'].value;
        var name = $(".itemName", this).text();
        $('#modalTextId').append("Dodaj komentarz dla " + name);
        $('#modalTextId').after(dbId);
        $("#commentSubmit").attr("dbId", dbId) //Tak brzydko...
    }
});
$(document).on("click", ".send-comment", function(e){
    var txt = $("#comment").val();  
    var rating = $("#rating").val();
    var idDb = $("#commentSubmit").attr("dbId");
    var postPath = "/commentary/" + idDb;
    $.post(postPath, {commentText: txt}, function(result){
         alert("ay");
    });
});
</script>

{{>topMenu}}
{{>itemsList}}